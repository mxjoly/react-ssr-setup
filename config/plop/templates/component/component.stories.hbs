import React from 'react';
import { Story, Meta } from '@storybook/react/types-6-0';

import {{properCase componentName}}, { {{properCase componentName}}Props } from '.';

export default {
  component: {{properCase componentName}},
  title: '{{titleCase componentType}}/{{properCase componentName}}',
  parameters: {
    docs: {
      description: {
        component: 'This is the description of the easier component ever built.',
      },
    },
  },
  args: {
    // Default props passed to all the stories
  },
} as Meta;

const Template: Story<{{properCase componentName}}Props> = (args: {{properCase componentName}}Props) => <{{properCase componentName}} {...args} />;

export const Default = Template.bind({});
Default.storyName = 'Default';

export const WithAnId = Template.bind({});
WithAnId.storyName = 'With an id';
WithAnId.args = { id: '0123456789' };
WithAnId.parameters = {
  docs: {
    description: {
      story: 'This component could have an id.',
    },
  },
};
