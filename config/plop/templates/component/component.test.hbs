import React from 'react';
import { shallow, ShallowWrapper } from 'enzyme';

import {{properCase componentName}}, { {{properCase componentName}}Props, classNames } from './index';

describe('{{properCase componentName}}', () => {
    let component: ShallowWrapper;
    let initialProps: {{properCase componentName}}Props;

    beforeEach(() => {
        initialProps = {
            id: '12345',
        };
        component = shallow(< {{properCase componentName}} {...initialProps} />);
    });

    afterEach(() => {
        jest.clearAllMocks();
    });

    afterAll(() => {
        jest.restoreAllMocks();
    });

    it('renders without errors', () => {
        expect(component.hasClass(classNames.ROOT)).toBe(true);
    });

    it('should match its reference snapshot', () => {
        expect(component.html()).toMatchSnapshot();
    });
});