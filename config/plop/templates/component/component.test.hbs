import React from 'react';
import { shallow } from 'enzyme';

import {{properCase componentName}} from '.';

describe('{{properCase componentName}}', () => {
    const defaultProps = {};

    it('renders without crashing', () => {
        shallow(< {{properCase componentName}} {...defaultProps} />);
    });
});